import * as React from 'react';
import Card from '@mui/material/Card';

function ImgMediaCard() {
    return (
        <Card sx={{ maxWidth: 750, margin: '50px auto' }}>
            Here's a code
        </Card>
    );
}

export default ImgMediaCard;